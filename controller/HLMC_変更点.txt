% 入力生成
obj.param.input.range : 最大のみ　→　最大最小の4x2
obj.input.input_TH : loop内での入力の最大最小
sigma同様に評価値によって値を変動させる
上下限を設定することで入力が少し滑らかになった

% 最適化
QPの行列計算部分の追加：コンストラクタ

% 目標値
HLの目標値として計算できていない？？
-> 実状態（仮想状態の順番）- ref とした

% parameter
H=10でないとうまくいかない？
N=100でも飛んでしまう...
